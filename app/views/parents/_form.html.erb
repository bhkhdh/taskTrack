<%= form_with(model: parent, local: true) do |form| %>
  <% if parent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parent.errors.count, "error") %> prohibited this parent from being saved:</h2>

      <ul>
      <% parent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :full_name %>
    <%= form.text_field :full_name, id: :parent_full_name %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.number_field :balance, id: :parent_balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
